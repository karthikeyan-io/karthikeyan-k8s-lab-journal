####################################################################

Advance_jsonpath_commands

####################################################################

controlplane ~ ➜  kubectl get nodes -o json > /opt/outputs/nodes.json

controlplane ~ ➜  kubectl get nodes node01 -o json > /opt/outputs/node01.json

controlplane ~ ➜  kubectl get nodes -o=jsonpath='{.items[0].metadata.name}'
controlplane
controlplane ~ ➜  kubectl get nodes -o=jsonpath='{.items[*].metadata.name}'
controlplane node01
controlplane ~ ➜  kubectl get nodes -o=jsonpath='{.items[*].metadata.name}' > /opt/outputs/node_names.txt

controlplane ~ ➜  

controlplane ~ ➜  kubectl get nodes -o=jsonpath='{.items[*].status.nodeInfo.osImage}' 
Ubuntu 22.04.5 LTS Ubuntu 22.04.5 LTS
controlplane ~ ➜  

controlplane ~ ➜  kubectl get nodes -o=jsonpath='{.items[*].status.nodeInfo.osImage}' > /opt/outputs/nodes_os.txt

controlplane ~ ➜  kubectl config view --kubeconfig=/root/my-kube-config 
apiVersion: v1
clusters:
- cluster:
    certificate-authority: /etc/kubernetes/pki/ca.crt
    server: KUBE_ADDRESS
  name: development
- cluster:
    certificate-authority: /etc/kubernetes/pki/ca.crt
    server: KUBE_ADDRESS
  name: kubernetes-on-aws
- cluster:
    certificate-authority: /etc/kubernetes/pki/ca.crt
    server: KUBE_ADDRESS
  name: production
- cluster:
    certificate-authority: /etc/kubernetes/pki/ca.crt
    server: KUBE_ADDRESS
  name: test-cluster-1
contexts:
- context:
    cluster: kubernetes-on-aws
    user: aws-user
  name: aws-user@kubernetes-on-aws
- context:
    cluster: test-cluster-1
    user: dev-user
  name: research
- context:
    cluster: development
    user: test-user
  name: test-user@development
- context:
    cluster: production
    user: test-user
  name: test-user@production
current-context: test-user@development
kind: Config
users:
- name: aws-user
  user:
    client-certificate: /etc/kubernetes/pki/users/aws-user/aws-user.crt
    client-key: /etc/kubernetes/pki/users/aws-user/aws-user.key
- name: dev-user
  user:
    client-certificate: /etc/kubernetes/pki/users/dev-user/developer-user.crt
    client-key: /etc/kubernetes/pki/users/dev-user/dev-user.key
- name: test-user
  user:
    client-certificate: /etc/kubernetes/pki/users/test-user/test-user.crt
    client-key: /etc/kubernetes/pki/users/test-user/test-user.key

controlplane ~ ➜  

controlplane ~ ➜  

controlplane ~ ➜  kubectl config view --kubeconfig=my-kube-config -o jsopath="{.users[*].name}" > /opt/outputs/users.txt
error: unable to match a printer suitable for the output format "jsopath={.users[*].name}", allowed formats are: go-template,go-template-file,json,jsonpath,jsonpath-as-json,jsonpath-file,kyaml,name,template,templatefile,yaml

controlplane ~ ✖ kubectl config view --kubeconfig=my-kube-config  -o jsonpath="{.users[*].name}" > /opt/outputs/users.txt

controlplane ~ ➜  kubectl get pv
NAME       CAPACITY   ACCESS MODES   RECLAIM POLICY   STATUS      CLAIM   STORAGECLASS   VOLUMEATTRIBUTESCLASS   REASON   AGE
pv-log-1   100Mi      RWX            Retain           Available                          <unset>                          21m
pv-log-2   200Mi      RWX            Retain           Available                          <unset>                          21m
pv-log-3   300Mi      RWX            Retain           Available                          <unset>                          21m
pv-log-4   40Mi       RWX            Retain           Available                          <unset>                          21m

controlplane ~ ➜  kubectl get pv --sort-by=.spec.capacity.storage 
NAME       CAPACITY   ACCESS MODES   RECLAIM POLICY   STATUS      CLAIM   STORAGECLASS   VOLUMEATTRIBUTESCLASS   REASON   AGE
pv-log-4   40Mi       RWX            Retain           Available                          <unset>                          22m
pv-log-1   100Mi      RWX            Retain           Available                          <unset>                          22m
pv-log-2   200Mi      RWX            Retain           Available                          <unset>                          22m
pv-log-3   300Mi      RWX            Retain           Available                          <unset>                          22m

controlplane ~ ➜  



controlplane ~ ➜  kubectl get pv --sort-by=.spec.capacity.storage -o=custom-columns=NAME:.metadata.name,CAPACITY:.spec.capacity.storage > /opt/outputs/pv-and-capacity-sorted.txt

controlplane ~ ➜  

controlplane ~ ➜  

controlplane ~ ➜  kubectl get context
error: the server doesn't have a resource type "context"

controlplane ~ ✖ kubectl config view --kubeconfig=my-kube-config -o jsonpath="{.contexts[?(@.context.user=='aws-user')].name}" > /opt/outputs/aws-context-name

controlplane ~ ➜  

controlplane ~ ➜  cat /opt/outputs/aws-context-name
aws-user@kubernetes-on-aws
controlplane ~ ➜  
